import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import App from './components';

const Root = () => (
  <BrowserRouter>
    <App />
  </BrowserRouter>
);

export default Root;
